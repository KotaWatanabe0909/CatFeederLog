# CLAUDE.md — CatFeederLog

## コードとデータの分離原則

このプロジェクトはオープンソースとして公開する。  
**個人・環境に依存する情報は一切コードに書いてはいけない。**

### コードに書かないもの（DB または環境変数で管理）

- 猫の名前・品種・生年月日
- フードの種類・ブランド名
- 給餌スケジュール（時間・回数）
- メンバーの名前・メールアドレス
- Supabase の URL・API キー等のシークレット

### 環境変数（`.env.local`）で管理するもの

- `NEXT_PUBLIC_SUPABASE_URL`
- `NEXT_PUBLIC_SUPABASE_ANON_KEY`
- `SUPABASE_SERVICE_ROLE_KEY`
- その他サードパーティの API キー

`.env.local` は `.gitignore` に含め、絶対にコミットしない。

### 設計の方針

- ユーザーが作成した「世帯（household）」単位でデータを管理するマルチテナント構成にする
- 誰でもこのリポジトリをデプロイすれば、自分の猫用として独立して使えることを担保する
- 個人データを含むサンプルコード・コメント・テストデータも書かない
